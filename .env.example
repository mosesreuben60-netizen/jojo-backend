# Copy this file to .env for local development.
# On Railway, set these under your service's Variables tab instead —
# don't commit a real .env file.

# Provided automatically by Railway's MongoDB plugin if you add one,
# otherwise paste your MongoDB Atlas connection string here.
MONGO_URL=mongodb://localhost:27017/jojo

# Any long random string — used to sign rider login tokens.
JWT_SECRET=change-this-to-something-long-and-random

# The rider's admin login. Run `npm run hash-password` locally to
# generate ADMIN_PASSWORD_HASH from a plain-text password.
ADMIN_EMAIL=rider@example.com
ADMIN_PASSWORD_HASH=

# Paystack secret key (starts with sk_test_ or sk_live_) — used
# server-side to verify payments. Never put this in frontend code.
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxx

# Comma-separated list of origins allowed to call this API
# (your GitHub Pages URL, plus http://localhost:5500 or similar while testing).
ALLOWED_ORIGINS=https://mosesreuben60-netizen.github.io,http://localhost:5500

PORT=3000
